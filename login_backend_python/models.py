def find_user_by_username(username):
    # Stub function to simulate user lookup
    class User:
        def __init__(self, name):
            self.name = name
            self.password = "dummy_password_hash"
    return User(username)
